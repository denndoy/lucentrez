version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '127.0.0.1:6969:6969'
    env_file:
      - .env
    networks:
      - lucentrez-network
    restart: unless-stopped

networks:
  lucentrez-network:
    external: true